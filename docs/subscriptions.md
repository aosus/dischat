# Subscriptions

Supported commands:

- `/watch category`
- `/watch category <slug>`
- `/watch all`
- `/unwatch category <slug>`
- `/unwatch all`
- `/watches`

Production mode watches public categories only. Live E2E mode must be explicitly restricted to category `56`.

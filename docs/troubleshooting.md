# Troubleshooting

- If startup fails, check missing environment variables first.
- If `mkdocs build --strict` fails, fix broken docs links or missing files.
- If live tests try to target the wrong category, the service should fail before the write.
